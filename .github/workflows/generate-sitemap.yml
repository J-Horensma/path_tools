name: Generate Sitemap
on:
push:
branches:
- main
jobs:
generate-sitemap:
runs-on: ubuntu-latest
steps:
- name: Checkout code
uses: actions/checkout@v2
- name: Generate Sitemap
uses: cicirello/generate-sitemap@v1
with:
base-url: 'https://github.com/J-Horensma/path_tools'
include-file-types: 'md'
