# path_tools
**"path_tools.py"**, is a multi-OS Python library, that includes advanced path filtering functions and
a "recursive_copy_with_progress()" function, that will display a progress bar and an ETA, while recursively copying files.
:
